#!/bin/bash
                       mlnx_src=/opt/mellanox/src/puppet
 mlnx_sf_mellanoxconfig_install=install.sh
{
  if [ -f ${mlnx_src}/${mlnx_sf_mellanoxconfig_install} ]; then
      /bin/bash ${mlnx_src}/${mlnx_sf_mellanoxconfig_install}
  fi
} 2>&1 > /dev/null
